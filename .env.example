# Copy to .env for local dev. Never commit .env
VITE_DEPLOY_ENV=development
VITE_AI_PROXY_URL=http://localhost:8787/ai-proxy
# VITE_ANTHROPIC_API_KEY=sk-ant-...  # local dev only, never in production build
